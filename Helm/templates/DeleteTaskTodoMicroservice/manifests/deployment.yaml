apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-todo-delete-tasks-api
spec:
  replicas: {{ .Values.deletetask.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-todo-delete-tasks-api
  template:
    metadata:
      name: {{ .Release.Name }}-todo-delete-tasks
      labels:
        app: {{ .Release.Name }}-todo-delete-tasks-api
    spec:
      containers:
       - name: {{ .Release.Name }}-todo-delete-tasks-api
         image: {{ .Values.common.acr_name }}/{{ .Values.deletetask.image.image_name }}:{{ .Values.deletetask.image.version }}
         ports:
          - containerPort: {{ .Values.deletetask.ports.containerPort }}
         env: 
           - name: {{ .Values.common.env.name }}
             value: {{ .Values.common.env.value}}
