apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-todo-delete-tasks-api-ingress
spec:
  ingressClassName: {{ .Values.common.ingressClassName }}
  rules:
    - host: {{ .Values.deletetask.host }}
      http:
        paths:
          - pathType: {{ .Values.deletetask.pathType }}
            path: {{ .Values.deletetask.path}}
            backend:
              service:
                name: {{ .Release.Name }}-todo-delete-tasks-api
                port:
                  number: {{ .Values.deletetask.ports.port }}
                  
