apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-todo-get-task-api
spec:
  replicas: {{ .Values.gettask.replicas }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-todo-get-task-api
  template:
    metadata:
      name: {{ .Release.Name }}-todo-get-task
      labels:
        app: {{ .Release.Name }}-todo-get-task-api
    spec:
      containers:
      - name: {{ .Release.Name }}-todo-get-task-api
        image: {{ .Values.common.acr_name }}/{{ .Values.gettask.image.image_name }}:{{ .Values.gettask.image.version }}
        ports:
        - containerPort: {{ .Values.gettask.ports.containerPort }}
        env: 
        - name: {{ .Values.common.env.name}}
          value: {{ .Values.common.env.value }}
          

