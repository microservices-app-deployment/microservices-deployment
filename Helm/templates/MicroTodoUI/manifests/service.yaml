apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-todo-ui
spec:
  type: {{ .Values.todoui.type }}
  selector:
    app: {{ .Release.Name }}-todo-ui
  ports:
    - port: {{ .Values.todoui.ports.port }}
      targetPort: {{ .Values.todoui.ports.port }}
